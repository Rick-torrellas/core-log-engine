[project]
name = "core_log_engine"
version = "0.2.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gitchangelog>=3.0.4",
    "pytest>=9.0.2",
    "ruff>=0.15.4",
]
